# =============================================================================
# Phase-3 Todo AI Chatbot Backend - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# Required variables are marked with (REQUIRED)
# Optional variables show their default values

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# PostgreSQL database connection string with asyncpg driver (REQUIRED)
# Format: postgresql+asyncpg://username:password@host:port/database?sslmode=require
# Example for Neon: postgresql+asyncpg://user:pass@ep-xxx.us-east-1.aws.neon.tech/neondb?sslmode=require
DATABASE_URL=postgresql+asyncpg://user:password@host/database?sslmode=require

# =============================================================================
# AUTHENTICATION CONFIGURATION
# =============================================================================

# JWT secret key for Better Auth token verification (REQUIRED)
# Generate a secure random string (minimum 32 characters)
# Example: openssl rand -base64 32
BETTER_AUTH_SECRET=your-secret-key-here-minimum-32-characters

# Better Auth base URL for fetching JWKS (optional, required in production)
# This should match your frontend deployment URL
# Local development: http://localhost:3000
# Production: https://your-app.vercel.app
BETTER_AUTH_URL=http://localhost:3000

# =============================================================================
# AI PROVIDER CONFIGURATION (Google Gemini)
# =============================================================================

# Google Gemini API key (REQUIRED)
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# Gemini API base URL (optional, default shown)
# Use the OpenAI-compatible endpoint for Gemini
GEMINI_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai/

# Gemini model name (optional, default shown)
# Available models: gemini-2.5-flash, gemini-2.0-flash, gemini-1.5-pro
GEMINI_MODEL_NAME=gemini-2.5-flash

# =============================================================================
# AI AGENT CONFIGURATION
# =============================================================================

# AI agent display name (optional, default: "Todo Assistant")
AI_AGENT_NAME=Todo Assistant

# AI agent system instructions (optional, has default)
# Customize the AI's behavior and personality
AI_AGENT_INSTRUCTIONS=You are a helpful todo management assistant. Help users manage their tasks using the available tools. Always confirm destructive actions like deletions before proceeding.

# =============================================================================
# API TIMEOUT & RETRY CONFIGURATION
# =============================================================================

# Request timeout in seconds (optional, default: 30)
REQUEST_TIMEOUT=30

# Maximum number of retries for failed requests (optional, default: 3)
MAX_RETRIES=3

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================
# Note: These are not read from environment in current config
# They are shown here for reference only

# API server host (default: localhost)
# API_HOST=localhost

# API server port (default: 8000)
# API_PORT=8000

# =============================================================================
# OPTIONAL: MCP SERVER CONFIGURATION
# =============================================================================
# These are optional and only needed if running a separate MCP server

# MCP_SERVER_HOST=localhost
# MCP_SERVER_PORT=8000
# MCP_SERVER_PATH=/mcp

# =============================================================================
# DEVELOPMENT & DEBUGGING
# =============================================================================

# Set to 'true' to enable debug mode (optional)
# DEBUG=false

# Log level (optional, default: INFO)
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# LOG_LEVEL=INFO

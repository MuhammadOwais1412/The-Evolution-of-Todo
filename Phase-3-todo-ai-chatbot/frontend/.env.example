# =============================================================================
# Phase-3 Todo AI Chatbot Frontend - Environment Configuration
# =============================================================================
# Copy this file to .env.local and fill in your actual values
# Required variables are marked with (REQUIRED)

# =============================================================================
# BACKEND API CONFIGURATION
# =============================================================================

# Backend API base URL (REQUIRED)
# Local development: http://localhost:8000
# Production: https://your-backend-api.com
NEXT_PUBLIC_API_URL=http://localhost:8000

# =============================================================================
# BETTER AUTH CONFIGURATION
# =============================================================================

# Better Auth secret key for JWT signing (REQUIRED - Server-side only)
# Generate a secure random string (minimum 32 characters)
# Example: openssl rand -base64 32
# IMPORTANT: Keep this secret! Never expose in client-side code
BETTER_AUTH_SECRET=change-me-in-production-use-secure-random-string

# Better Auth URL - Server-side (REQUIRED)
# This is used by the server-side auth configuration
# Local development: http://localhost:3000
# Production: https://your-app.vercel.app
BETTER_AUTH_URL=http://localhost:3000

# Better Auth URL - Client-side (REQUIRED)
# This is exposed to the browser and used by the auth client
# Should match BETTER_AUTH_URL in most cases
# Local development: http://localhost:3000
# Production: https://your-app.vercel.app
NEXT_PUBLIC_BETTER_AUTH_URL=http://localhost:3000

# JWT token expiration time (optional, default: 7d)
# Format: number + unit (s=seconds, m=minutes, h=hours, d=days)
# Examples: 30m, 2h, 7d, 30d
NEXT_PUBLIC_BETTER_AUTH_JWT_EXPIRES_IN=7d

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# PostgreSQL database connection string (REQUIRED)
# This is used by Better Auth for storing user data, sessions, etc.
# Format: postgresql://username:password@host:port/database?sslmode=require
#
# For Neon PostgreSQL:
# You can use either DATABASE_URL or NEON_DATABASE_URL (both work)
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Alternative: Neon-specific variable name (optional)
# NEON_DATABASE_URL=postgresql://user:password@ep-xxx.us-east-1.aws.neon.tech/neondb?sslmode=require

# =============================================================================
# OPTIONAL: DEVELOPMENT & DEBUGGING
# =============================================================================

# Node environment (automatically set by Next.js)
# NODE_ENV=development

# Enable verbose logging (optional)
# NEXT_PUBLIC_DEBUG=false

# =============================================================================
# DEPLOYMENT NOTES
# =============================================================================
#
# For Vercel deployment:
# 1. Add all environment variables in Vercel dashboard
# 2. Ensure BETTER_AUTH_SECRET is different from development
# 3. Update BETTER_AUTH_URL and NEXT_PUBLIC_BETTER_AUTH_URL to production URL
# 4. Update NEXT_PUBLIC_API_URL to production backend URL
# 5. Ensure DATABASE_URL points to production database
#
# Security checklist:
# - Never commit .env.local to git
# - Use different secrets for development and production
# - Rotate secrets regularly
# - Use environment-specific database instances
